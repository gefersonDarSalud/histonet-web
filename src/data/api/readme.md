🌐 Manejo de la Conexión HTTP pura.
Se encarga únicamente de construir las URLs, manejar los headers, ejecutar la llamada (usando fetch, axios, etc.) y devolver la respuesta bruta (cuerpo JSON, errores HTTP).
